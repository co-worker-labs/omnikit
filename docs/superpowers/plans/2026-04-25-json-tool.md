# JSON Format / Compress Tool — Implementation Plan

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a JSON formatting/compression tool with JSON5 support to ByteCraft

**Architecture:** Single page component (`json-page.tsx`) with in-component business logic (no separate libs/ files needed — all logic is specific to this tool). Follows existing `urlencoder-page.tsx` pattern: Conversion + Description + Layout wrapper.

**Tech Stack:** Next.js 16, TypeScript, Tailwind CSS 4, next-intl, json5, Lucide icons

**Note:** This project has no automated test framework. Verification is via `npm run build` (type-check + lint) and manual QA.

---

## Task 1: Install Dependency & Register Tool

**Files:**

- Modify: `libs/tools.ts`
- Modify: `i18n/request.ts`
- Run: `npm install json5`

- [ ] **Step 1: Install json5 package**

```bash
npm install json5
```

- [ ] **Step 2: Register tool in `libs/tools.ts`**

Insert `{ key: "json", path: "/json" }` into the `TOOLS` array, after the existing `checksum` entry. Position is cosmetic (it controls homepage card order) — what matters is **not removing any existing entry**.

```ts
// libs/tools.ts — insert one line after the checksum entry:
{ key: "json", path: "/json" },
```

Full TOOLS array after change (verify all 11 existing entries remain — `checksum` in particular):

```ts
export const TOOLS: { key: string; path: string }[] = [
  { key: "base64", path: "/base64" },
  { key: "urlencoder", path: "/urlencoder" },
  { key: "uuid", path: "/uuid" },
  { key: "password", path: "/password" },
  { key: "hashing", path: "/hashing" },
  { key: "checksum", path: "/checksum" },
  { key: "json", path: "/json" },
  { key: "htmlcode", path: "/htmlcode" },
  { key: "storageunit", path: "/storageunit" },
  { key: "ascii", path: "/ascii" },
  { key: "cipher", path: "/cipher" },
  { key: "diff", path: "/diff" },
] as const;
```

- [ ] **Step 3: Add namespace to `i18n/request.ts`**

Append `"json"` to the `namespaces` array (alphabetical order, after `"hashing"`):

```ts
const namespaces = [
  "common",
  "tools",
  "home",
  "password",
  "hashing",
  "json",
  "base64",
  // ... rest unchanged
];
```

Full array:

```ts
const namespaces = [
  "common",
  "tools",
  "home",
  "password",
  "hashing",
  "json",
  "base64",
  "ascii",
  "htmlcode",
  "checksum",
  "cipher",
  "storageunit",
  "terms",
  "privacy",
  "uuid",
  "urlencoder",
  "diff",
];
```

---

## Task 2: Create i18n Translation Files

**Files:**

- Create: `public/locales/en/json.json`
- Create: `public/locales/zh-CN/json.json`
- Create: `public/locales/zh-TW/json.json`

- [ ] **Step 1: Create English translations — `public/locales/en/json.json`**

```json
{
  "input": "JSON Input",
  "output": "JSON Output",
  "inputPlaceholder": "Paste or type your JSON here",
  "format": "Format",
  "compress": "Compress",
  "indent": "Indent",
  "useTab": "Use Tab",
  "sortKeys": "Sort Keys",
  "json5": "JSON5",
  "backfill": "Fill to Input",
  "formatted": "Formatted",
  "compressed": "Compressed",
  "relaxedParse": "Relaxed JSON5 parsing applied",
  "filled": "Filled to input",
  "invalid": "Invalid JSON",
  "largeFileWarning": "Large JSON files may cause browser lag",
  "descriptions": {
    "whatIsTitle": "What is JSON Formatting?",
    "whatIsP1": "JSON (JavaScript Object Notation) is the universal data interchange format of the web. APIs return it, config files use it, and every modern programming language can parse it. Formatting (pretty-printing) takes minified, hard-to-read JSON and adds indentation and line breaks so humans can scan and understand the structure at a glance.",
    "whatIsP2": "Compression (minification) does the reverse: it strips all unnecessary whitespace, producing the smallest possible valid JSON for production use — smaller payloads mean faster network transfers and lower bandwidth costs.",
    "whatIsP3": "This tool runs entirely in your browser. No data is ever sent to any server.",
    "json5Title": "What is JSON5?",
    "json5P1": "JSON5 is a superset of JSON that adds human-friendly features: comments (// and /* */), trailing commas, single-quoted strings, unquoted object keys, hexadecimal numbers (0xFF), and special values like NaN and Infinity. It was created to make JSON easier to write by hand — especially for configuration files.",
    "json5P2": "Enable JSON5 mode to parse relaxed input directly. When JSON5 mode is off, Format and Compress automatically fall back to JSON5 parsing if strict JSON parsing fails — you'll see a notification when this happens. Output is always standard JSON.",
    "useCasesTitle": "Common Use Cases",
    "useCasesP1": "Formatting API responses: paste a raw API response and pretty-print it to understand the data structure.",
    "useCasesP2": "Minifying config files: compress JSON before deploying to production to reduce file size.",
    "useCasesP3": "Fixing hand-edited JSON: paste JSON with trailing commas or comments, toggle JSON5 mode, and get clean standard JSON output.",
    "useCasesP4": "Sorting object keys: alphabetically sort keys for consistent diff output when version-controlling JSON files.",
    "limitationsTitle": "Limitations",
    "limitationsP1": "Very large JSON files (>10MB) may cause the browser to pause briefly during formatting.",
    "limitationsP2": "Numbers beyond JavaScript's safe integer range (±2⁵³) may lose precision after round-tripping through JSON.parse/stringify.",
    "limitationsP3": "JSON5 error messages may not include exact line and column numbers."
  }
}
```

- [ ] **Step 2: Create Simplified Chinese translations — `public/locales/zh-CN/json.json`**

```json
{
  "input": "JSON 输入",
  "output": "JSON 输出",
  "inputPlaceholder": "在此粘贴或输入 JSON",
  "format": "格式化",
  "compress": "压缩",
  "indent": "缩进",
  "useTab": "使用制表符",
  "sortKeys": "键排序",
  "json5": "JSON5",
  "backfill": "回填到输入",
  "formatted": "已格式化",
  "compressed": "已压缩",
  "relaxedParse": "已使用 JSON5 宽松解析",
  "filled": "已填入输入",
  "invalid": "无效 JSON",
  "largeFileWarning": "大文件可能导致浏览器卡顿",
  "descriptions": {
    "whatIsTitle": "什么是 JSON 格式化？",
    "whatIsP1": "JSON（JavaScript Object Notation）是网络通用数据交换格式。API 返回 JSON，配置文件使用 JSON，所有现代编程语言都可以解析 JSON。格式化（美化打印）将压缩的、难以阅读的 JSON 添加缩进和换行，使结构一目了然。",
    "whatIsP2": "压缩（最小化）则相反：去除所有不必要的空白字符，生成最小的有效 JSON 用于生产环境——更小的负载意味着更快的网络传输和更低的带宽成本。",
    "whatIsP3": "此工具完全在浏览器中运行，不会将任何数据发送到服务器。",
    "json5Title": "什么是 JSON5？",
    "json5P1": "JSON5 是 JSON 的超集，增加了人性化特性：注释（// 和 /* */）、尾随逗号、单引号字符串、无引号键名、十六进制数字（0xFF）以及 NaN 和 Infinity 等特殊值。它的设计目标是让手工编写 JSON 更加容易——尤其适合配置文件。",
    "json5P2": "开启 JSON5 模式后直接解析宽松输入。当 JSON5 关闭时，如果严格 JSON 解析失败，格式化和压缩会自动降级使用 JSON5 解析——你会看到相应提示。输出始终为标准 JSON。",
    "useCasesTitle": "常见使用场景",
    "useCasesP1": "格式化 API 响应：粘贴原始 API 响应并美化输出，理解数据结构。",
    "useCasesP2": "压缩配置文件：部署到生产前压缩 JSON 以减小文件大小。",
    "useCasesP3": "修复手动编辑的 JSON：粘贴带尾随逗号或注释的 JSON，开启 JSON5 模式，获取干净的标准 JSON 输出。",
    "useCasesP4": "对键排序：按字母顺序排序键名，在版本控制 JSON 文件时获得一致的差异输出。",
    "limitationsTitle": "局限性",
    "limitationsP1": "非常大的 JSON 文件（>10MB）可能导致浏览器短暂停顿。",
    "limitationsP2": "超出 JavaScript 安全整数范围（±2⁵³）的数字在经过 JSON.parse/stringify 往返后可能丢失精度。",
    "limitationsP3": "JSON5 错误消息可能不包含精确的行号和列号。"
  }
}
```

- [ ] **Step 3: Create Traditional Chinese translations — `public/locales/zh-TW/json.json`**

```json
{
  "input": "JSON 輸入",
  "output": "JSON 輸出",
  "inputPlaceholder": "在此貼上或輸入 JSON",
  "format": "格式化",
  "compress": "壓縮",
  "indent": "縮排",
  "useTab": "使用 Tab",
  "sortKeys": "鍵排序",
  "json5": "JSON5",
  "backfill": "回填到輸入",
  "formatted": "已格式化",
  "compressed": "已壓縮",
  "relaxedParse": "已使用 JSON5 寬鬆解析",
  "filled": "已填入輸入",
  "invalid": "無效 JSON",
  "largeFileWarning": "大檔案可能導致瀏覽器延遲",
  "descriptions": {
    "whatIsTitle": "什麼是 JSON 格式化？",
    "whatIsP1": "JSON（JavaScript Object Notation）是網路通用資料交換格式。API 回傳 JSON，設定檔使用 JSON，所有現代程式語言都可以解析 JSON。格式化（美化列印）將壓縮、難以閱讀的 JSON 加入縮排和換行，使結構一目瞭然。",
    "whatIsP2": "壓縮則相反：去除所有不必要的空白字元，產生最小的有效 JSON 用於生產環境——更小的負載意味著更快的網路傳輸和更低的頻寬成本。",
    "whatIsP3": "此工具完全在瀏覽器中執行，不會將任何資料傳送到伺服器。",
    "json5Title": "什麼是 JSON5？",
    "json5P1": "JSON5 是 JSON 的超集合，增加了人性化特性：註解（// 和 /* */）、尾隨逗號、單引號字串、無引號鍵名、十六進位數字（0xFF），以及 NaN 和 Infinity 等特殊值。它的設計目標是讓手寫 JSON 更加容易——尤其適合設定檔。",
    "json5P2": "開啟 JSON5 模式後直接解析寬鬆輸入。當 JSON5 關閉時，如果嚴格 JSON 解析失敗，格式化和壓縮會自動降級使用 JSON5 解析——你會看到相應提示。輸出始終為標準 JSON。",
    "useCasesTitle": "常見使用情境",
    "useCasesP1": "格式化 API 回應：貼上原始 API 回應並美化輸出，理解資料結構。",
    "useCasesP2": "壓縮設定檔：部署到生產前壓縮 JSON 以減小檔案大小。",
    "useCasesP3": "修復手動編輯的 JSON：貼上帶尾隨逗號或註解的 JSON，開啟 JSON5 模式，取得乾淨的標準 JSON 輸出。",
    "useCasesP4": "對鍵名排序：按字母順序排序鍵名，在版本控制 JSON 檔案時獲得一致的差異輸出。",
    "limitationsTitle": "局限性",
    "limitationsP1": "非常大的 JSON 檔案（>10MB）可能導致瀏覽器短暫停頓。",
    "limitationsP2": "超出 JavaScript 安全整數範圍（±2⁵³）的數字在經過 JSON.parse/stringify 往返後可能遺失精度。",
    "limitationsP3": "JSON5 錯誤訊息可能不包含精確的行號和列號。"
  }
}
```

---

## Task 3: Add Tool Entries to tools.json

**Files:**

- Modify: `public/locales/en/tools.json`
- Modify: `public/locales/zh-CN/tools.json`
- Modify: `public/locales/zh-TW/tools.json`

- [ ] **Step 1: Add entry to `public/locales/en/tools.json`**

Add the following entry into the JSON object (existing keys are not in alphabetical or any meaningful order — placement does not affect rendering, just keep valid JSON):

```json
"json": {
  "title": "JSON Formatter / Compressor - Free Online Tool",
  "shortTitle": "JSON Format / Compress",
  "description": "Format, compress, and validate JSON online. Pretty-print with configurable indentation, minify to single line, or parse JSON5. 100% client-side."
},
```

- [ ] **Step 2: Add entry to `public/locales/zh-CN/tools.json`**

Add the following entry into the JSON object (existing keys are not in alphabetical or any meaningful order — placement does not affect rendering, just keep valid JSON):

```json
"json": {
  "title": "JSON 格式化 / 压缩 - 在线工具",
  "shortTitle": "JSON 格式化",
  "description": "在线格式化、压缩和验证 JSON。支持自定义缩进美化输出、单行压缩和 JSON5 宽松解析。100% 浏览器端处理。"
},
```

- [ ] **Step 3: Add entry to `public/locales/zh-TW/tools.json`**

Add the following entry into the JSON object (existing keys are not in alphabetical or any meaningful order — placement does not affect rendering, just keep valid JSON):

```json
"json": {
  "title": "JSON 格式化 / 壓縮 - 線上工具",
  "shortTitle": "JSON 格式化",
  "description": "線上格式化、壓縮和驗證 JSON。支援自訂縮排美化輸出、單行壓縮和 JSON5 寬鬆解析。100% 瀏覽器端處理。"
},
```

---

## Task 4: Create Route Entry (page.tsx)

**Files:**

- Create: `app/[locale]/json/page.tsx`

- [ ] **Step 1: Create `app/[locale]/json/page.tsx`**

```tsx
import { getTranslations } from "next-intl/server";
import JsonPage from "./json-page";

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale, namespace: "tools" });
  return {
    title: t("json.title"),
    description: t("json.description"),
    keywords: "",
  };
}

export default function JsonRoute() {
  return <JsonPage />;
}
```

---

## Task 5: Create Page Component (json-page.tsx)

**Files:**

- Create: `app/[locale]/json/json-page.tsx`

- [ ] **Step 1: Create `app/[locale]/json/json-page.tsx`**

```tsx
"use client";

import { useState, useEffect } from "react";
import json5 from "json5";
import { ChevronsDown, ChevronsUp, X } from "lucide-react";
import { useTranslations } from "next-intl";

import Layout from "../../../components/layout";
import { CopyButton } from "../../../components/ui/copy-btn";
import { Button } from "../../../components/ui/button";
import { StyledTextarea, StyledCheckbox } from "../../../components/ui/input";
import { showToast } from "../../../libs/toast";

// --- Types ---

type IndentSize = 2 | 4 | 8;

type JsonError = {
  message: string;
  line?: number;
  column?: number;
};

type ParseResult = {
  value: unknown;
  usedRelaxed: boolean;
};

// --- Core Logic ---

function tryParse(input: string, json5Mode: boolean): ParseResult {
  if (json5Mode) {
    return { value: json5.parse(input), usedRelaxed: true };
  }
  try {
    return { value: JSON.parse(input), usedRelaxed: false };
  } catch {
    return { value: json5.parse(input), usedRelaxed: true };
  }
}

function deepSortKeys(value: unknown): unknown {
  if (Array.isArray(value)) return value.map(deepSortKeys);
  if (value !== null && typeof value === "object") {
    const sorted: Record<string, unknown> = {};
    for (const key of Object.keys(value as Record<string, unknown>).sort()) {
      sorted[key] = deepSortKeys((value as Record<string, unknown>)[key]);
    }
    return sorted;
  }
  return value;
}

function stringify(value: unknown, sortKeys: boolean, indent: string | number): string {
  const target = sortKeys ? deepSortKeys(value) : value;
  return JSON.stringify(target, undefined, indent);
}

function extractError(e: SyntaxError): JsonError {
  // json5 attaches lineNumber/columnNumber directly on the SyntaxError object.
  // V8's JSON.parse encodes "(line L column C)" inside the message string.
  const e2 = e as SyntaxError & { lineNumber?: number; columnNumber?: number };
  if (typeof e2.lineNumber === "number") {
    return { message: e.message, line: e2.lineNumber, column: e2.columnNumber };
  }
  const lc = e.message.match(/line (\d+) column (\d+)/i);
  if (lc) {
    return { message: e.message, line: Number(lc[1]), column: Number(lc[2]) };
  }
  return { message: e.message };
}

// --- Conversion Component ---

function Conversion() {
  const t = useTranslations("json");
  const tc = useTranslations("common");

  const [rawContent, setRawContent] = useState("");
  const [outputContent, setOutputContent] = useState("");
  const [indentSize, setIndentSize] = useState<IndentSize>(2);
  const [useTab, setUseTab] = useState(false);
  const [sortKeys, setSortKeys] = useState(false);
  const [json5Mode, setJson5Mode] = useState(false);
  const [error, setError] = useState<JsonError | null>(null);

  // Deferred validation — 500ms debounce with cleanup
  useEffect(() => {
    const timer = setTimeout(() => {
      if (!rawContent.trim()) {
        setError(null);
        return;
      }
      try {
        if (json5Mode) {
          json5.parse(rawContent);
        } else {
          JSON.parse(rawContent);
        }
        setError(null);
      } catch (e) {
        if (e instanceof SyntaxError) {
          setError(extractError(e));
        }
      }
    }, 500);
    return () => clearTimeout(timer);
  }, [rawContent, json5Mode]);

  function doFormat() {
    const input = rawContent.trim();
    if (!input) return;
    try {
      const { value, usedRelaxed } = tryParse(input, json5Mode);
      const indent = useTab ? "\t" : indentSize;
      const out = stringify(value, sortKeys, indent);
      setOutputContent(out);
      if (usedRelaxed && !json5Mode) {
        showToast(t("relaxedParse"), "info", 3000);
      } else {
        showToast(t("formatted"), "success", 2000);
      }
    } catch (e) {
      if (e instanceof SyntaxError) {
        setError(extractError(e));
        showToast(t("invalid"), "danger", 3000);
      }
    }
  }

  function doCompress() {
    const input = rawContent.trim();
    if (!input) return;
    try {
      const { value, usedRelaxed } = tryParse(input, json5Mode);
      const target = sortKeys ? deepSortKeys(value) : value;
      const out = JSON.stringify(target);
      setOutputContent(out);
      if (usedRelaxed && !json5Mode) {
        showToast(t("relaxedParse"), "info", 3000);
      } else {
        showToast(t("compressed"), "success", 2000);
      }
    } catch (e) {
      if (e instanceof SyntaxError) {
        setError(extractError(e));
        showToast(t("invalid"), "danger", 3000);
      }
    }
  }

  function doClearAll() {
    setRawContent("");
    setOutputContent("");
    setError(null);
    showToast(tc("allCleared"), "danger", 2000);
  }

  function doBackfill() {
    setRawContent(outputContent);
    showToast(t("filled"), "success", 2000);
  }

  function handleJson5Toggle(checked: boolean) {
    setJson5Mode(checked);
    setOutputContent("");
  }

  const isDisabledAction = !rawContent.trim();
  const isDisabledClear = !rawContent.trim() && !outputContent.trim();
  const isOutputEmpty = !outputContent.trim();

  const INDENT_SIZES: IndentSize[] = [2, 4, 8];

  return (
    <section id="conversion">
      {/* Input Area */}
      <div>
        <div className="flex flex-wrap justify-between items-center">
          <div className="flex items-center gap-2">
            <span className="w-2.5 h-2.5 rounded-full bg-accent-cyan/60" />
            <span className="font-mono text-sm font-semibold text-accent-cyan">{t("input")}</span>
          </div>
          <button
            type="button"
            className="text-danger text-xs hover:text-danger/80 transition-colors cursor-pointer"
            onClick={() => {
              setRawContent("");
              setError(null);
              showToast(tc("cleared"), "danger", 2000);
            }}
          >
            {tc("clear")}
          </button>
        </div>
        <div className="relative mt-1">
          <StyledTextarea
            id="rawContentTextarea"
            placeholder={t("inputPlaceholder")}
            rows={10}
            value={rawContent}
            onChange={(e) => setRawContent(e.target.value)}
            className="font-mono text-sm"
          />
          <CopyButton getContent={() => rawContent} className="absolute end-2 top-2" />
        </div>
      </div>

      {/* Options Row */}
      <div className="mt-4 flex flex-wrap items-center gap-4">
        {/* Indent radio group */}
        <div className="flex items-center gap-2">
          <span className="font-mono text-xs font-semibold text-fg-muted">{t("indent")}</span>
          <div
            role="radiogroup"
            aria-label={t("indent")}
            className={`inline-flex rounded-full border border-border-default overflow-hidden ${
              useTab ? "opacity-40 pointer-events-none" : ""
            }`}
          >
            {INDENT_SIZES.map((n) => (
              <button
                key={n}
                type="button"
                role="radio"
                aria-checked={indentSize === n}
                disabled={useTab}
                onClick={() => setIndentSize(n)}
                className={
                  "px-3 py-1 text-sm font-semibold transition-colors cursor-pointer " +
                  (indentSize === n
                    ? "bg-accent-cyan text-bg-base"
                    : "bg-transparent text-fg-secondary hover:bg-bg-elevated")
                }
              >
                {n}
              </button>
            ))}
          </div>
        </div>

        <StyledCheckbox
          label={t("useTab")}
          id="useTabCheck"
          checked={useTab}
          onChange={(e) => setUseTab(e.target.checked)}
        />
        <StyledCheckbox
          label={t("sortKeys")}
          id="sortKeysCheck"
          checked={sortKeys}
          onChange={(e) => setSortKeys(e.target.checked)}
        />
        <StyledCheckbox
          label={t("json5")}
          id="json5Check"
          checked={json5Mode}
          onChange={(e) => handleJson5Toggle(e.target.checked)}
        />
      </div>

      {/* Action Buttons */}
      <div className="mt-4 grid grid-cols-3 gap-3 items-center">
        <Button
          variant="primary"
          size="md"
          disabled={isDisabledAction}
          onClick={doFormat}
          className="rounded-full font-bold"
        >
          {t("format")}
          <ChevronsDown size={16} className="ms-1" />
        </Button>
        <Button
          variant="primary"
          size="md"
          disabled={isDisabledAction}
          onClick={doCompress}
          className="rounded-full font-bold"
        >
          {t("compress")}
          <ChevronsUp size={16} className="ms-1" />
        </Button>
        <Button
          variant="danger"
          size="md"
          disabled={isDisabledClear}
          onClick={doClearAll}
          className="rounded-full font-bold"
        >
          {tc("clearAll")}
          <X size={16} className="ms-1" />
        </Button>
      </div>

      {/* Error Display */}
      {error && (
        <div role="alert" aria-live="polite" className="text-danger text-sm mt-2">
          ⚠ {error.message}
          {error.line != null &&
            ` (line ${error.line}${error.column != null ? `, col ${error.column}` : ""})`}
        </div>
      )}

      {/* Output Area */}
      <div className="mt-4">
        <div className="flex flex-wrap justify-between items-center">
          <div className="flex items-center gap-2">
            <span className="w-2.5 h-2.5 rounded-full bg-accent-purple/60" />
            <span className="font-mono text-sm font-semibold text-accent-purple">
              {t("output")}
            </span>
          </div>
          <div className="flex items-center gap-3">
            {!isOutputEmpty && (
              <button
                type="button"
                className="text-accent-cyan text-xs hover:text-accent-cyan/80 transition-colors cursor-pointer"
                onClick={doBackfill}
              >
                {t("backfill")}
              </button>
            )}
            <button
              type="button"
              className="text-danger text-xs hover:text-danger/80 transition-colors cursor-pointer"
              onClick={() => {
                setOutputContent("");
                showToast(tc("cleared"), "danger", 2000);
              }}
            >
              {tc("clear")}
            </button>
          </div>
        </div>
        <div className="relative mt-1">
          <StyledTextarea
            id="outputContentTextarea"
            placeholder=""
            rows={10}
            value={outputContent}
            readOnly
            className="font-mono text-sm"
          />
          <CopyButton getContent={() => outputContent} className="absolute end-2 top-2" />
        </div>
      </div>
    </section>
  );
}

// --- Description Component ---

function Description() {
  const t = useTranslations("json");

  return (
    <section id="description" className="mt-8">
      <div className="mb-4">
        <h5 className="font-semibold text-fg-primary text-base">{t("descriptions.whatIsTitle")}</h5>
        <div className="mt-1 space-y-1.5 text-fg-secondary text-sm leading-relaxed">
          <p>{t("descriptions.whatIsP1")}</p>
          <p>{t("descriptions.whatIsP2")}</p>
          <p>{t("descriptions.whatIsP3")}</p>
        </div>
      </div>

      <div className="mb-4">
        <h5 className="font-semibold text-fg-primary text-base">{t("descriptions.json5Title")}</h5>
        <div className="mt-1 space-y-1.5 text-fg-secondary text-sm leading-relaxed">
          <p>{t("descriptions.json5P1")}</p>
          <p>{t("descriptions.json5P2")}</p>
        </div>
      </div>

      <div className="mb-4">
        <h5 className="font-semibold text-fg-primary text-base">
          {t("descriptions.useCasesTitle")}
        </h5>
        <div className="mt-1 space-y-1.5 text-fg-secondary text-sm leading-relaxed">
          <p>{t("descriptions.useCasesP1")}</p>
          <p>{t("descriptions.useCasesP2")}</p>
          <p>{t("descriptions.useCasesP3")}</p>
          <p>{t("descriptions.useCasesP4")}</p>
        </div>
      </div>

      <div className="mb-4">
        <h5 className="font-semibold text-fg-primary text-base">
          {t("descriptions.limitationsTitle")}
        </h5>
        <div className="mt-1 space-y-1.5 text-fg-secondary text-sm leading-relaxed">
          <p>{t("descriptions.limitationsP1")}</p>
          <p>{t("descriptions.limitationsP2")}</p>
          <p>{t("descriptions.limitationsP3")}</p>
        </div>
      </div>
    </section>
  );
}

// --- Page Component ---

export default function JsonPage() {
  const tc = useTranslations("common");
  const t = useTranslations("tools");
  const title = t("json.shortTitle");

  return (
    <Layout title={title}>
      <div className="container mx-auto px-4 pt-3 pb-6">
        <div className="flex items-start gap-2 border-l-2 border-accent-cyan bg-accent-cyan-dim/30 rounded-r-lg p-3 my-4">
          <span className="text-sm text-fg-secondary leading-relaxed">
            {tc("alert.notTransferred")}
          </span>
        </div>

        <Conversion />
        <Description />
      </div>
    </Layout>
  );
}
```

---

## Task 6: Build & Verify

- [ ] **Step 1: Run build**

```bash
npm run build
```

Expected: Build succeeds with no TypeScript errors and no ESLint errors.

- [ ] **Step 2: Verify file structure**

```bash
ls -la app/[locale]/json/
ls -la public/locales/en/json.json
ls -la public/locales/zh-CN/json.json
ls -la public/locales/zh-TW/json.json
```

Expected: All files exist.

- [ ] **Step 3: Manual QA checklist**

Start dev server (`npm run dev`) and verify at `http://localhost:3000/json`:

| Test Case                   | Steps                                                       | Expected                                                                               |
| --------------------------- | ----------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Basic format                | Paste `{"a":1,"b":2}`, click Format                         | Output: `{\n  "a": 1,\n  "b": 2\n}`                                                    |
| Basic compress              | Paste pretty-printed JSON, click Compress                   | Output: single-line JSON                                                               |
| Sort keys                   | Paste `{"c":3,"a":1,"b":2}`, enable Sort Keys, click Format | Keys ordered: a, b, c                                                                  |
| Use tab                     | Enable Use Tab, click Format                                | Output uses `\t` indentation                                                           |
| Indent 4                    | Select Indent 4, click Format                               | Output uses 4-space indent                                                             |
| Indent disabled             | Enable Use Tab                                              | Indent radio group grayed out                                                          |
| Empty input                 | No text entered                                             | Format/Compress disabled                                                               |
| Copy input                  | Click copy icon on input                                    | Content copied to clipboard                                                            |
| Copy output                 | Format, click copy icon on output                           | Output copied to clipboard                                                             |
| Backfill                    | Format, click "Fill to Input"                               | Output copied to input textarea                                                        |
| Clear All                   | Click Clear All                                             | Both textareas cleared                                                                 |
| JSON5 mode — comments       | Enable JSON5, paste `{/*comment*/"a":1}`, click Format      | Valid JSON output                                                                      |
| JSON5 mode — trailing comma | Enable JSON5, paste `{"a":1,}`, click Format                | `{"a":1}`                                                                              |
| Auto-fallback               | JSON5 off, paste `{"a":1,}` (trailing comma), click Format  | Toast "Relaxed JSON5 parsing applied", valid output                                    |
| Error display (JSON)        | Paste `{invalid`, wait 500ms                                | Red error message with `(line X, col Y)` appended                                      |
| Error display (JSON5)       | Enable JSON5, paste `{\n\nabc`, wait 500ms                  | Error includes line/col extracted from json5 SyntaxError (`lineNumber`/`columnNumber`) |
| JSON5 toggle clears output  | Format something, toggle JSON5                              | Output textarea clears                                                                 |
| zh-CN route                 | Navigate to `/zh-CN/json`                                   | Chinese UI renders                                                                     |
| zh-TW route                 | Navigate to `/zh-TW/json`                                   | Traditional Chinese UI renders                                                         |
| Homepage tool card          | Navigate to `/`                                             | JSON card appears in tool list                                                         |

---

## Plan Self-Review

- [x] **Spec coverage**: All spec sections mapped to tasks — dependencies (T1), i18n (T2-T3), routing (T4), UI/logic (T5), verification (T6)
- [x] **Placeholder scan**: No TBD/TODO markers. All code blocks are complete. All i18n prose is written.
- [x] **Type consistency**: `IndentSize`, `JsonError`, `ParseResult` types defined in T5 and used consistently throughout. i18n keys match between json.json files and component usage.
- [x] **Boundary cases addressed**: Empty input (disabled buttons), whitespace (trim), primitives (handled by JSON.parse), error display (line/column with null check), useTab disables indent, JSON5 toggle clears output, debounce cleanup, auto-fallback toast.
- [x] **No regressions to existing tools**: T1 Step 2 preserves all 11 existing TOOLS entries (especially `checksum`).
- [x] **Error position extraction verified against real implementations**: V8 `JSON.parse` exposes `(line L column C)` in the message; `json5.parse` attaches `lineNumber`/`columnNumber` directly on the SyntaxError object — `extractError(e)` handles both. Position offsets are NOT shown as columns.
- [x] **Accessibility**: Error display uses `role="alert"` + `aria-live="polite"` so screen readers announce debounced errors.
- [x] **Toast consistency**: `Clear All` uses `tc("allCleared")` matching the urlencoder pattern; no duplicate `cleared` key in `json.json`.
